import React from 'react';

const StarRating = () => {
  return (
    <div>
      {[...Array(5)].map(star => {
        // this method requires react-icons
      })}
    </div>
  );
};

export default StarRating;
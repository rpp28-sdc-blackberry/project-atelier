import React from 'react';

//This likely doesn't need state either, just render data passed down from parent list component
const ItemCard = (props) => (
  <div>
    item card
  </div>
);

export default ItemCard;